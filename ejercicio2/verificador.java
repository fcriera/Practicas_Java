package ejercicio2;

public class verificador {
    public verificador(){
    }
    public String verificaProducto(){
        String verifica = "";
        Producto prod = new Producto(prod.getNombre(), prod.getPrecioUnitario());
        while (verifica == ""){
            try{
                
            } catch (In)

        }

    }
}
